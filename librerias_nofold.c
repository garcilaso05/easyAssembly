// mostrar_raiz.c
#include <stdio.h>
#include <math.h>

int main() {
    double numero = 49.0;
    double raiz = sqrt(numero);

    printf("La raíz cuadrada de %.1f es %.1f\n", numero, raiz);
    return 0;
}
